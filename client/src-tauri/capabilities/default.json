{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capabilities for the DSH Client loading page",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "autostart:default",
    "updater:default",
    "window-state:default"
  ]
}
